<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center_horizontal"
    android:layout_margin="10dp"
    android:gravity="center_horizontal"
    android:orientation="horizontal"
    android:padding="5dp">

    <ImageView
        android:id="@+id/itemIcon_ImageView"
        android:layout_width="54dp"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:contentDescription="@string/itemView_alt_image"
        android:foregroundGravity="center"
        app:srcCompat="@drawable/ic_house_stark"
        tools:ignore="VectorDrawableCompat" />

    <Button
        android:id="@+id/itemName_Button"
        style="?android:attr/borderlessButtonStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="2dp"
        android:gravity="left"
        android:text="@string/itemViewText_defaultLabel"
        android:textColor="#33234E" />
</LinearLayout>